{
  "name": "aws-infra",
  "version": "0.1.0",
  "bin": {
    "simple-src-cdk": "Main.js"
  },
  "scripts": {
    "postinstall": "(cd ./lambda && npm install);",
    "build": "tsc --noEmit",
    "watch": "tsc --watch --noEmit",
    "test": "jest",
    "cog-poc": "cdk --profile cognito-poc",
    "synth": "npm run cog-poc synth",
    "diff": "npm run cog-poc diff",
    "bootstrap": "npm run cog-poc bootstrap",
    "deploy": "cdk --all --profile cognito-poc deploy",
    "deploy-teststack": "npm run cog-poc deploy SimpleTestStack",
    "deploy-lambda": "npm run cog-poc deploy LambdaStackV3",
    "deploy-dev": "npm run cog-poc deploy CloudFrontStackV3 CognitoGoogleStackV3",
    "destroy-dev": "cdk --profile cognito-poc destroy CloudFrontStackV3 CognitoGoogleStackV3",
    "deploy-cloudfront": "npm run cog-poc deploy CloudFrontStackV2",
    "deploy-cloudfront3": "npm run cog-poc deploy CloudFrontStackV3",
    "hotswap-cloudfront3": "cdk --hotswap --profile cognito-poc deploy CloudFrontStackV3",
    "deploy-cog-google": "npm run cog-poc deploy CognitoGoogleStackV2",
    "deploy-cog-email": "npm run cog-poc deploy CognitoEmailStack",
    "hotswap-lambda": "cdk --hotswap --profile cognito-poc deploy LambdaStack",
    "destroy-all": "cdk --all --profile cognito-poc donot-destroy",
    "doctor": "npm run cog-poc doctor",
    "context": "npm run cog-poc context",
    "outputs": "cdk --all --profile cognito-poc --outputs-file cdk.out/outputs.json deploy"
  },
  "devDependencies": {
    "esbuild": "0.14.29",
    "@types/jest": "27.4.1",
    "@types/node": "10.17.27",
    "jest": "27.5.1",
    "ts-jest": "27.1.4",
    "aws-cdk": "2.19.0",
    "ts-node": "10.7.0",
    "tsconfig-paths": "3.14.1",    
    "typescript": "4.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.19.0",
    "constructs": "10.0.102",
    "source-map-support": "0.5.21"
  }
}
